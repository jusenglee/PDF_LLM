# 핵심 의존성
PyMuPDF==1.23.8  # PDF 처리
aiohttp==3.9.1    # 비동기 HTTP 통신
transformers==4.36.2  # 텍스트 토크나이징
numpy>=1.22.0    # 수치 연산

# 의미 검색 모듈용 (선택적)
sentence-transformers>=2.2.2  # 문장 임베딩
faiss-cpu>=1.7.4  # 벡터 유사도 검색

# 유틸리티
logging>=0.5.1.2  # 로깅
tqdm>=4.66.1     # 진행률 표시
